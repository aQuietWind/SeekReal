<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seekreal.appreciation.Mapper.LikeMapper">
    <!--    查询用户点赞列表的文章id-->
    <select id="getLikeWritingIdList" resultType="java.lang.Long">
        select writing_id from Like_Writing where user_id=#{userId} and is_like=1 limit #{start},#{number}
    </select>
    <!--    查询用户点赞列表的提问id-->
    <select id="getLikeQuestionIdList" resultType="java.lang.Long">
        select question_id from Like_Question where user_id=#{userId} and is_like=1 limit #{start},#{number}
    </select>
</mapper>