<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seekreal.social.Mapper.FollowMapper">
    <!--    查询用户关注列表的用户id-->
    <select id="getLikerIdList" resultType="java.lang.Long">
        select liker_id from Social_Follow where user_id=#{userId} and is_follow=1 limit #{start},#{number}
    </select>
    <!--    查询用户粉丝列表的用户id-->
    <select id="getFollowerIdList" resultType="java.lang.Long">
        select user_id from Social_Follow where liker_id=#{userId} and is_follow=1 limit #{start},#{number}
    </select>
</mapper>